@model HaberSitesi.Web.Areas.Admin.Models.EtiketModel

@{
    ViewBag.Title = "Etiket Ekle";
}

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Etiket Ekleme</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.Ad)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Ad)
            @Html.ValidationMessageFor(model => model.Ad)
        </div>

        <p>
            <input type="submit" value="Kaydet" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Listeye geri dön", "Etiketler")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
